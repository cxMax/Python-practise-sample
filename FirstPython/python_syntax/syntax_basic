1. 标识符
_foo 的代表不能直接访问的类属性, 需通过类提供的接口进行访问
__foo 代表类的私有成员
__foo__ 代表 Python 里特殊方法专用的标识 , 例如 : __init__()

2. 行和缩进
Python 的代码块不使用大括号 {} 来控制类
严格的缩进

3. 多行语句
使用斜杠（ \）将一行的语句分为多行显示

4. 引号
引号( ' )、双引号( " )、三引号( ''' 或 """ ) 

5. 注释
单行注释采用 # 开头。
多行注释使用三个单引号(''')或三个双引号(""")

6. 等待用户输入
raw_input({$words})

7. 输出
print({$words})

8. 同一行显示多条语句
```
import sys; x = "saonima"; sys.stdout.write(x);
```
