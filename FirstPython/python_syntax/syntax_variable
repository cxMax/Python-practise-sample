1. 多个变量赋值
```
a = b = c = 1
e, f, g = 1, 2, "hello world"
```

2. 标准数据类型
Numbers（数字） , String（字符串） , List（列表） , Tuple（元组） , Dictionary（字典）

3. Numbers（数字）
使用del语句删除一些对象的引用

4. String（字符串）
加号（+）是字符串连接运算符，星号（*）是重复操作

5. List（列表）
列表用 [ ] 标识，是 python 最通用的复合数据类型
列表是允许更新的
加号（+）是字连接运算符，星号（*）是重复操作

6. Tuple（元组）
元组用"()"标识。内部元素用逗号隔开。但是元组不能二次赋值，相当于只读列表
元组是不允许更新的

7. Dictionary（字典）
无序的对象集合 , 跟java的Set和Map相像
字典用"{ }"标识。字典由索引(key)和它对应的值value组成

8. 数据类型转换
通过内置函数进行数据类型转换
